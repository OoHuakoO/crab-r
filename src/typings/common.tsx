export interface Toast {
    open: boolean;
    text: string;
}

export interface LoginState {
    role: string;
    token: string;
}

export interface ListCountCrab {
    count: string;
}
